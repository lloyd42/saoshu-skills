interface:
  display_name: "扫书MCP增强适配"
  short_description: "调用MCP/外部工具增强角色关系标签，失败自动回退本地"
  default_prompt: "Use $saoshu-mcp-enricher-adapter to enrich batch metadata with external MCP outputs and fallback safely."
